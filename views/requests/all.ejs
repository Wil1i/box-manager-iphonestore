<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
  <%- include("../templates/header.ejs", {title : "آیفون استور", css : "homePage"}) %>
</head>
<body>
  <%- include("../templates/sideBar.ejs") %>
  <div id="check-container">
    <div>
      <h1>درخواست‌های جعبه‌ها</h1>
    </div>
    
    <div>
        <% requests.filter(s => s.status.includes("ناموجود")).forEach(request => { %>
            <%- include("../templates/request.ejs", {request}) -%>
        <% }) %>
        <% requests.filter(s => !s.status.includes("ناموجود")).forEach(request => { %>
          <%- include("../templates/request.ejs", {request}) -%>
      <% }) %>
    </div>
  </div>
</body>
</html>